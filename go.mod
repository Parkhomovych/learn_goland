// go.mod - це основний файл керування модулями в Go
// Він визначає ім'я модуля, версію Go та залежності проекту
// Цей файл автоматично оновлюється командами go mod, go get тощо

// module визначає шлях імпорту для цього модуля
// Цей шлях використовується при імпорті пакетів з вашого модуля
// Формат: module [шлях_модуля]
module demo/app

// go визначає мінімальну версію Go, необхідну для цього модуля
// Це гарантує, що код буде компілюватися тільки з сумісною версією Go
// Формат: go [версія]
go 1.24.0

// Далі можуть бути наступні секції (зараз відсутні):

// require - список залежностей та їх версій
// Приклад:
// require (
//     github.com/example/package v1.2.3
//     github.com/another/package v2.3.4
// )

// replace - заміна модулів на локальні версії або інші модулі
// Приклад:
// replace example.com/package => ../local/package

// exclude - виключення певних версій модулів
// Приклад:
// exclude github.com/user/package v1.2.3
